{% doc %}
  Description of block role and behavior.

  **Execution context**: `block.settings`, `block.id`, `block.shopify_attributes`.
  **DOM zones**: `__media`, `__content-column`, `__main`, `__footer`.
  **Sub-blocks**: `ndev-media`, `ndev-text`, `ndev-price`.

  @param {product} [prod] - Optional product override
  @param {variant} [var] - Optional variant override

  @example
  In parent: `{ "type": "my-block" }` — configure in theme editor.
{% enddoc %}
